{
  "name": "scara-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for SCARA robot HMI and ESP32 firmware",
  "workspaces": [
    "hmi",
    "firmware"
  ],
  "scripts": {
    "gen": "node shared/telemetry/generate.mjs",
    "dev": "npm run dev -w hmi-ta",
    "build": "npm run build -w hmi-ta",
    "fw:build": "npm run build -w scara-firmware",
    "fw:upload": "npm run upload -w scara-firmware",
    "fw:monitor": "npm run monitor -w scara-firmware"
  }
}
